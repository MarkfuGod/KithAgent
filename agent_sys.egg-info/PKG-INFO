Metadata-Version: 2.4
Name: agent-sys
Version: 0.1.0
Summary: AgentOS — A system-level agent daemon that maps traditional OS concepts to LLM agent architecture
License: MIT
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: pyyaml>=6.0
Provides-Extra: full
Requires-Dist: watchdog>=4.0; extra == "full"
Requires-Dist: aiohttp>=3.9; extra == "full"
Provides-Extra: dev
Requires-Dist: pytest>=8.0; extra == "dev"
Requires-Dist: pytest-asyncio>=0.23; extra == "dev"

# AgentOS — SysAgent

**把传统操作系统的架构映射到 LLM Agent 系统上。**

SysAgent 是一个常驻后台的系统级 Agent 守护进程。它开机即运行，持续索引你的文件和数据，构建个人知识库。当你使用 Cursor、Claude Code、OpenClaw 等 Agent 工具时，它们可以直接调用 SysAgent，而无需每次重新扫描文件系统。

## 核心思想：OS → AgentOS 映射

```
┌─────────────────────────────────────────────────────────────┐
│                    External Agents                          │
│            (Cursor / Claude Code / OpenClaw)                │
│                         │                                   │
│                    ┌────▼────┐                              │
│                    │ Syscall │  ← 系统调用层 (API)          │
│                    │  API    │    Unix Socket / HTTP         │
│                    └────┬────┘                              │
│                         │                                   │
│  ┌──────────────────────▼──────────────────────────┐       │
│  │              Kernel (SysAgent Daemon)            │       │
│  │                                                  │       │
│  │  ┌───────────┐  ┌───────────┐  ┌────────────┐  │       │
│  │  │ Scheduler │  │  Memory   │  │ FileSystem │  │       │
│  │  │ (调度器)   │  │ (记忆存储) │  │ (文件监控)  │  │       │
│  │  └─────┬─────┘  └───────────┘  └────────────┘  │       │
│  │        │                                        │       │
│  │  ┌─────▼─────────────────────────────┐         │       │
│  │  │       Agent Thread Pool            │         │       │
│  │  │  [FileSearch] [Knowledge] [Context]│         │       │
│  │  └───────────────────────────────────┘         │       │
│  └─────────────────────────────────────────────────┘       │
│                                                             │
│                  ~/.agent_sys/memory.db                     │
│                    (持久化知识库)                             │
└─────────────────────────────────────────────────────────────┘
```

| 传统 OS 概念 | AgentOS 对应 | 说明 |
|---|---|---|
| **内核 (Kernel)** | `SysAgentKernel` | 守护进程，管理所有子系统生命周期 |
| **线程 (Thread)** | `AgentTask` | 被调度器分配的最小执行单元 |
| **进程调度器** | `AgentScheduler` | 优先级队列 + 并发控制 |
| **文件系统 (VFS)** | `FileSystemWatcher` | 实时监控 + 定时全量扫描 |
| **内存/缓存** | `MemoryStore` | LRU 热缓存 + SQLite 冷存储 |
| **系统调用 (Syscall)** | `SyscallServer` | Unix Socket / HTTP API |
| **系统调用号** | `SyscallType` | file.search, knowledge.query 等 |

## 快速开始

```bash
# 安装
cd agent_sys
pip install -e ".[full]"

# 启动 SysAgent（前台模式，可看到日志）
agent-sys start

# 或作为后台守护进程启动
agent-sys start -d

# 检查状态
agent-sys ping
agent-sys status

# 搜索已索引的文件
agent-sys search "database config"
agent-sys search "api handler" --type .py

# 查询知识库
agent-sys query --category preferences

# 停止
agent-sys stop
```

## 在外部 Agent 中调用

### Python SDK（异步）

```python
from src.syscall.client import SysAgentClient

async with SysAgentClient(caller="cursor") as client:
    # 搜索文件 — 不用重新遍历文件系统
    results = await client.file_search("database migration")

    # 查询用户偏好 — 不用每次重新分析
    prefs = await client.knowledge_query(category="preferences")

    # 保存会话上下文 — 下次 Agent 启动可恢复
    await client.context_save("session-abc", {"topic": "refactoring auth module"})

    # 加载上一次的上下文
    ctx = await client.context_load("session-abc")
```

### Python SDK（同步，适用于插件环境）

```python
from src.syscall.client import SyncSysAgentClient

with SyncSysAgentClient(caller="my_plugin") as client:
    results = client.file_search("config parser")
```

### HTTP API

```bash
# Ping
curl http://127.0.0.1:7437/health

# Status
curl http://127.0.0.1:7437/status

# Syscall
curl -X POST http://127.0.0.1:7437/syscall \
  -H "Content-Type: application/json" \
  -d '{"call_type": "file.search", "params": {"query": "todo"}, "caller": "curl"}'
```

## 自定义 Agent（扩展线程池）

```python
from src.agents.base import BaseAgent, AgentTask

class MyCustomAgent(BaseAgent):
    name = "my_custom_task"

    async def execute(self, task: AgentTask, context: dict) -> dict:
        memory = context["memory"]
        # your logic here
        return {"result": "done"}

# 注册到调度器
scheduler.register_agent(MyCustomAgent())
```

## 配置

编辑 `config/default.yaml` 自定义：

- **监控目录**：哪些文件夹被索引
- **文件类型**：哪些扩展名被处理
- **并发数**：同时运行多少个 Agent 线程
- **传输方式**：Unix Socket / HTTP
- **扫描间隔**：全量重扫频率

## 项目结构

```
agent_sys/
├── config/
│   └── default.yaml          # 系统配置
├── src/
│   ├── kernel/                # 内核层
│   │   ├── daemon.py          # 守护进程主循环
│   │   └── config.py          # 配置加载
│   ├── filesystem/            # 文件系统层
│   │   └── watcher.py         # 文件监控与索引
│   ├── memory/                # 内存层
│   │   └── store.py           # LRU 缓存 + SQLite 持久化
│   ├── scheduler/             # 调度器层
│   │   └── pool.py            # 优先级队列 + 并发池
│   ├── syscall/               # 系统调用层
│   │   ├── protocol.py        # 消息协议定义
│   │   ├── server.py          # Socket/HTTP 服务端
│   │   └── client.py          # 客户端 SDK
│   ├── agents/                # Agent 线程池
│   │   ├── base.py            # 抽象基类
│   │   └── builtin.py         # 内置 Agent（文件搜索、知识查询等）
│   └── cli.py                 # 命令行入口
├── pyproject.toml
├── requirements.txt
└── README.md
```

## 设计哲学

传统计算机中，CPU 通过调度器分配线程来处理计算任务。在 AgentOS 中，**LLM 通过调度器分配 Agent 线程来处理智能任务**。

关键差异：
- OS 线程处理的是确定性的计算 → Agent 线程处理的是模糊的、需要理解的任务
- OS 用文件系统存储数据 → AgentOS 用知识库存储 **被理解的** 数据
- OS 的 syscall 是同步的函数调用 → AgentOS 的 syscall 是异步的消息传递
- OS 的内存是字节寻址 → AgentOS 的内存是语义寻址（按意义检索）

这不是要替代操作系统，而是在操作系统之上构建一个 **Agent 原生的运行时层**。
