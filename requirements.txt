# Core
pyyaml>=6.0
rich>=13.7

# Optional: real-time file watching (kqueue/FSEvents on macOS)
watchdog>=4.0

# Optional: HTTP API transport
aiohttp>=3.9

# Optional: LLM providers (install the ones you need)
openai>=1.0
anthropic>=0.30

# Optional: document extraction (install the ones you need)
PyMuPDF>=1.24       # PDF text extraction (import fitz)
python-docx>=1.1    # Word .docx extraction
